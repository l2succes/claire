import { Button } from '@/components/ui/Button';
import { Card } from '@/components/ui/Card';
import { SectionHeading } from '@/components/ui/SectionHeading';
import { homeFeatures, operatingModes } from '@/content/site';

export default function Home() {
  return <main><section className="home-hero shell"><div className="home-copy"><p className="eyebrow">THE AI-NATIVE MULTI-CHAT CLIENT</p><h1>All your chats.<br /><em>One AI.</em></h1><p>Claire brings your conversations together, understands the context across them, and helps you take the next useful action.</p><div className="button-row"><Button href="/download">Choose your setup</Button><Button href="/developers" tone="secondary">Explore the system</Button></div></div><div className="conversation-demo" aria-label="Claire product preview"><div className="demo-chrome"><span></span><span></span><span></span><b>ASK CLAIRE</b></div><div className="demo-question">What did I promise this week?</div><div className="demo-answer"><span className="sparkle">✦</span><div><b>You made three commitments.</b><p>Send Maya the proposal, confirm dinner with your sister, and review the launch copy with Alex.</p><div className="source-chips"><span>WhatsApp</span><span>Instagram</span><span>Telegram</span></div></div></div><div className="demo-action">Create reminders for all three <span>→</span></div></div></section><section className="feature-section shell"><SectionHeading eyebrow="ONE SYSTEM" title="Conversations become context—and context becomes action." body="Claire keeps the source visible while adding intelligence above every connected network." /><div className="card-grid">{homeFeatures.map(({ icon: Icon, title, body }, index) => <Card key={title} tint={index === 1 ? 'lime' : index === 2 ? 'sky' : 'paper'}><Icon className="feature-icon" /><h2>{title}</h2><p>{body}</p></Card>)}</div></section><section className="mode-section shell"><SectionHeading eyebrow="YOUR OPERATING MODEL" title="Pay us to run it. Or run it yourself." body="Hosting and AI are explicit choices, not hidden infrastructure decisions." /><div className="mode-grid">{operatingModes.map(({ icon: Icon, title, body }) => <div className="mode" key={title}><Icon /><div><h3>{title}</h3><p>{body}</p></div></div>)}</div><div className="button-row"><Button href="/pricing">Compare options</Button><Button href="/self-hosting" tone="quiet">Self-hosting guide</Button></div></section></main>;
}
